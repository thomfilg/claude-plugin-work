/**
 * Re-export of the brief-side memory plugin config so code-checker uses
 * the same cortex / mem0 detection contract.
 */

'use strict';

module.exports = require('../../work-brief/lib/memory-plugin-config');
