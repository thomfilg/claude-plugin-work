/**
 * Re-export of the brief-side memory plugin config so completion-checker
 * uses the same cortex / mem0 detection contract (BRIEF_MEMORY_* env vars).
 */

'use strict';

module.exports = require('../../work-brief/lib/memory-plugin-config');
