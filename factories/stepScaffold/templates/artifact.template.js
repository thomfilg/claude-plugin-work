'use strict';

/**
 * Step: {{id}} — generated by factories/stepScaffold (kind=artifact).
 */

const path = require('path');
const fs = require('fs');
const { createArtifactStep } = require(
  path.join(__dirname, '..', '..', '..', '..', '..', '..', 'factories', 'createArtifactStep')
);
const { STEPS } = require('../step-registry');

module.exports = createArtifactStep({
  id: STEPS.{{id}},
  artifact: '{{artifact}}',
  precondition: (s) => Boolean(s),
  artifactExists: (s, ctx) => fs.existsSync(path.join(ctx.tasksDir, '{{artifact}}')),
  command: '{{command}}',
  agentType: '{{agentType}}',
  agentPrompt: '{{command}}',
  injectPlanningContext: true,
});
