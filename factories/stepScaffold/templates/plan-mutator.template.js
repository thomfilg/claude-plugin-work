'use strict';

/**
 * Step: {{id}} — generated by factories/stepScaffold (kind=plan-mutator).
 *
 * Pseudo-step: does NOT emit its own plan entry. Patches sibling entries
 * in ctx.plan whose `step` is in `targetStepIds`.
 */

const path = require('path');
const { createPlanMutatorStep } = require(
  path.join(__dirname, '..', '..', '..', '..', '..', 'factories', 'createPlanMutatorStep')
);
const { STEPS } = require('../step-registry');

module.exports = createPlanMutatorStep({
  id: STEPS.{{id}},
  mutations: [
    {
      id: 'TODO-rename-me',
      targetStepIds: [STEPS.check],
      predicate: (_s, _ctx) => true,
      patch: (_entry, _s, _ctx) => ({ /* fields to merge */ }),
    },
  ],
});
